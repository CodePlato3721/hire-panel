# CR — feature

## Design

在 `CLAUDE.md` 中新增"单次改动颗粒度"规范，定义同层改动原则和一句话概括检验标准。

## Source Details

- `CLAUDE.md`：在 `## CR` 节之前新增 `## 单次改动颗粒度` 一节

## Source Tree

```
hire-panel/
└── CLAUDE.md    ← added 单次改动颗粒度 section
```

## Test Details

无。

## Test Tree

```
(无变更)
```
