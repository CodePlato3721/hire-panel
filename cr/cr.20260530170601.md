# CR — feature

## Design

在 CR 规范中明确 Chat 回显必须包含所有字段，不得省略。

## Source Details

- `CLAUDE.md`：`### CR（commit request）` 节新增"CR 回显规则"说明，强调 Chat 回显必须与 `.cr.md` 完全一致

## Source Tree

```
hire-panel/
└── CLAUDE.md    ← added CR echo completeness rule
```

## Test Details

纯文档改动，无需验证。

## Test Tree

```
(无变更)
```

## Test Result

人工测试，不提供 Test Result，具体验证方式见 Test Details。
